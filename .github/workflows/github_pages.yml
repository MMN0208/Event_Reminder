name: Auto Deploy to Github Page

on:
  push:
    branches:
      - main
  pull_request:
  
permissions:
  pull-requests: write
  issues: write
  repository-projects: write

jobs:
  deploy:
    runs-on: ubuntu-20.04
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: "14"

      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - run: npx browserslist@latest --update-db
      - run: npm ci
      - run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          github_token: ${{ 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 }}
          publish_dir: ./build
